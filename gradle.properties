# Gradle uses the JVM that launched it (CI sets up JDK 21; set a machine-specific
# org.gradle.java.home in ~/.gradle/gradle.properties for local builds, not here).
org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC
org.gradle.caching=true
org.gradle.configuration-cache=false

# Never try to auto-download a JDK toolchain (offline-friendly).
org.gradle.java.installations.auto-download=false

# Platform ships the Kotlin stdlib; don't bundle it into the plugin.
kotlin.stdlib.default.dependency=false
